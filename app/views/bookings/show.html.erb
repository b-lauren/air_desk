<div class="container">
  <h1><%= @listing.title %></h1>
    <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path @listing.photos[0].key, width: 302, height: 400, crop: :fill %>')">
    <p><%= @listing.description %> <%= @listing.rate %>

    </div>


<%= link_to "Back to Home", root_path %>

</div>

