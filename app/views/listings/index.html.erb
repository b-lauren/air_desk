<div class="main-container">
  <div class="index-cards py-4">
    <h4 class="text-center heading-varela p-4">View All of Our Desks</h4>
    <div class="cards-map rounded">
      <% @listings.each do |listing| %>
        <div class="card text-center">
          <%= link_to listing_path(listing) do %>
            <div class="listing-card">
              <%= cl_image_tag listing.photos[0].key, class: "listing-card-image"  %>
              <div class="card-body">
                <h5 class="card-title"><%= listing.title.capitalize %> </h5>
                <p>£<%= listing.rate %></p>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
  <div class="map-container">
    <div id="map"
       style="width: 100%; height: 100vh;"
       data-markers="<%= @markers.to_json %>"
       data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
  </div>
</div>

